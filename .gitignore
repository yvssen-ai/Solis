node_modules
dist
dist-ssr
.DS_Store
*.local
.vite
npm-debug.log*
yarn-error.log*

# Local secrets. VITE_* end up in the browser bundle, so nothing sensitive
# belongs here — but a stray key in a future .env should never reach the repo.
.env
.env.*
!.env.example
